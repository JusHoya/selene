navigation:
  # The grid spans the terrain EXACTLY: 500 x 500 cells at 1.0 m from
  # (-250, -250) is the same square as <size>500 500 ...</size> on
  # selene_sim/models/lunar_terrain/model.sdf and world.bounds in
  # selene_sim/config/world_params.yaml. That is not a coincidence to be
  # preserved by hand — selene_sim/test/test_world_extent_agrees.py fails the
  # build if any of those files stops describing the same square.
  grid_resolution: 1.0
  grid_width: 500
  grid_height: 500
  origin_x: -250.0
  origin_y: -250.0
  # How far inside the terrain edge a navigation goal must stay. AStarPlanner
  # refuses any goal outside it, which is the last guard before a robot is
  # commanded off the heightfield — past the edge there is no collision surface,
  # the body falls, and its AABB eventually leaves the integer range ODE's
  # broadphase uses, taking Gazebo down mid-mission. The derivation of the 10.0
  # (half a collision cell + robot half-extent + arrival slop + slack) is in
  # world_params.yaml beside `safety_margin_m`, which must carry the same value.
  terrain_margin_m: 10.0
  max_traversable_slope_deg: 15.0
  slope_penalty_weight: 2.0
  hazard_penalty_weight: 10.0
  obstacle_inflation_radius: 0.5

path_follower:
  lookahead_distance: 2.0
  waypoint_tolerance: 1.0
  linear_kp: 0.5
  angular_kp: 1.5
  max_angular_speed: 1.0
  stall_timeout_s: 5.0

obstacle_avoidance:
  detection_range: 5.0
  critical_range: 1.5
  avoidance_gain: 0.8
  slow_factor: 0.3
  depth_image_bins: 16

mission:
  prospect_waypoints:
    - [-60, -120]
    - [-80, -140]
    - [-100, -150]
    - [-110, -170]
    - [-90, -130]
  recharge_position: [-75.0, -100.0]
  low_battery_threshold: 0.15
  recharge_target: 0.9

static_obstacles:
  rocks:
    - {x: -55.0, y: -110.0, radius: 1.0}
    - {x: -72.0, y: -98.0, radius: 1.0}
    - {x: -140.0, y: -120.0, radius: 1.0}
    - {x: -125.0, y: -195.0, radius: 1.0}
    - {x: -60.0, y: -175.0, radius: 1.0}
    - {x: -88.0, y: -92.0, radius: 1.0}
    - {x: -48.0, y: -145.0, radius: 1.5}
    - {x: -150.0, y: -155.0, radius: 1.5}
    - {x: -105.0, y: -205.0, radius: 1.5}
    - {x: -95.0, y: -95.0, radius: 1.5}
    - {x: -68.0, y: -190.0, radius: 1.5}
    - {x: 30.0, y: -20.0, radius: 1.0}
    - {x: -180.0, y: 60.0, radius: 1.0}
    - {x: 120.0, y: -80.0, radius: 1.0}
    - {x: -40.0, y: 100.0, radius: 1.0}
    - {x: 80.0, y: 130.0, radius: 1.0}
    - {x: -150.0, y: -30.0, radius: 1.5}
    - {x: 160.0, y: 50.0, radius: 1.5}
    - {x: -20.0, y: -200.0, radius: 1.5}
    - {x: 100.0, y: -150.0, radius: 1.0}
    - {x: -90.0, y: 180.0, radius: 1.5}
    - {x: 200.0, y: -100.0, radius: 1.0}
    - {x: -10.0, y: -40.0, radius: 1.5}
    - {x: -15.0, y: -45.0, radius: 1.5}
    - {x: 180.0, y: -50.0, radius: 1.5}
    - {x: -50.0, y: 200.0, radius: 1.5}
