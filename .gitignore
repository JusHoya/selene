# Claude Code personal memory
.claude/

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg

# Test / coverage artifacts (pytest-cov writes one per package directory)
.coverage
.coverage.*
htmlcov/
.pytest_cache/

# ROS 2 build artifacts
install/
log/

# Node.js
node_modules/
selene_dashboard/build/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Environment / credentials
.env
.env.*
!.env.example

# MCP / Playwright artifacts
.mcp.json
.playwright-mcp/

# Docker
docker/.env

# Debug screenshots at repo root
/selene-*.png

# Patent / IP strategy working directories — private, not for public repo
deliverables/
docs/data/
docs/prompts/

# Patent / prior-art analysis — private, not for public repo.
# NOTE: this file is already tracked, so this rule alone does not untrack it.
# To stop tracking while keeping the local copy, run:
#   git rm --cached PATENT_PRIOR_ART_ANALYSIS.md
/PATENT_PRIOR_ART_ANALYSIS.md

# Presentation cheat sheets -- personal notes, never distributed, never committed.
IP_BOARD_ONEPAGER*.md
*.private.md

# Whitepaper build log -- regenerated artifact, not source.
docs/whitepapers/output/build.log
