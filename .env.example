# SELENE Environment Configuration
# Copy this to .env and fill in your values:
#   cp .env.example .env
#
# NEVER commit .env to git — it contains credentials.

# WSL2 sudo password (used by automated scripts)
WSL_SUDO_PASS=
